name: CI

on:
  pull_request:
  push:
    branches: [main]
    tags:
      - "v*"
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * *"

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:

  # =====================================================
  # 1. BUILD + TEST + LINT (Matrix)
  # =====================================================
  build:
    name: Build & Test (Go ${{ matrix.go }})
    runs-on: ubuntu-latest
    env:
      # Force older runners to auto-upgrade the toolchain to 1.25.5 while keeping native 1.25.5 unchanged
      GOTOOLCHAIN: go1.25.5

    strategy:
      fail-fast: false
      matrix:
        go: ["1.24.11","1.25.5"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache Go modules and build cache
        uses: actions/cache@v4
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
          key: go-cache-${{ runner.os }}-${{ matrix.go }}-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            go-cache-${{ runner.os }}-${{ matrix.go }}-
            go-cache-${{ runner.os }}-

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go }}
          cache: true

      - name: go mod tidy consistency
        run: |
          go mod tidy
          git diff --exit-code || (echo "❌ go mod tidy produced changes"; exit 1)

          

      - name: Verify formatting
        run: |
          if [ -n "$(gofmt -l .)" ]; then
            echo "❌ gofmt check failed:"
            gofmt -l .
            exit 1
          fi

      - name: Lint (golangci-lint)
        run: |
          # Using the official install script
          curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/HEAD/install.sh | sh -s -- -b $(go env GOPATH)/bin v2.6.2
          golangci-lint run --timeout=5m

      - name: Vulnerability scan (govulncheck)
        uses: golang/govulncheck-action@v1
        with:
          go-version-input: ${{ matrix.go }}

      - name: Verify fit-for-purpose plan present
        run: test -f docs/fit_for_purpose.poml

      - name: Run tests with coverage
        run: go test -coverprofile=coverage.out ./...

      - name: Verify DOT goldens
        run: |
          UPDATE_GOLDEN=0 go test ./poml -run TestRenderDOT

      - name: Enforce coverage ≥80%
        run: |
          THRESHOLD=80.0
          TOTAL=$(go tool cover -func=coverage.out | grep total | awk '{print substr($3, 1, length($3)-1)}')
          RESULT=$(printf "%.2f" "$TOTAL")
          echo "Coverage: $RESULT% (threshold: $THRESHOLD%)"
          if ! awk "BEGIN {exit ($RESULT < $THRESHOLD ? 1 : 0)}"; then
            echo "❌ Coverage $RESULT% below threshold"
            exit 1
          fi

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ matrix.go }}
          path: coverage.out

  # =====================================================
  # 2. E2E TESTS (Docker Matrix)
  # =====================================================
  e2e:
    name: POML Semantic E2E (Docker Matrix)
    runs-on: ubuntu-latest
    needs: build

    strategy:
      fail-fast: false
      matrix:
        base: ["debian:stable", "alpine:3.20", "ubuntu:24.04"]
        arch: ["amd64", "arm64"]

    steps:
      - uses: actions/checkout@v4
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to GitHub Container Registry
        env:
          CR_PAT: ${{ secrets.GITHUB_TOKEN }}
        run: echo "${CR_PAT}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Build Docker image
        run: |
          TAG=$(echo "${{ matrix.base }}" | tr ':' '-')
          ARCH="${{ matrix.arch }}"
          BASE="${{ matrix.base }}"
          cat <<'EOF' > Dockerfile
          ARG BASE_IMAGE=alpine:3.20
          FROM ${BASE_IMAGE}

          ARG GO_VERSION=1.25.5
          # Ensure curl/tar/git are available for Go install across alpine/apt-based images.
          RUN (apk add --no-cache curl tar git ca-certificates || \
               (apt-get update && apt-get install -y curl ca-certificates tar git)) && \
              update-ca-certificates || true
          RUN case "$(uname -s)" in \
                Linux) ;; \
                *) echo "unsupported OS"; exit 1 ;; \
              esac && \
              arch=$(uname -m) && \
              case "$arch" in x86_64) arch=amd64 ;; aarch64) arch=arm64 ;; armv7l) arch=armv6l ;; esac && \
              url="https://go.dev/dl/go${GO_VERSION}.linux-${arch}.tar.gz" && \
              curl -sSfL "$url" -o /tmp/go.tgz && \
              rm -rf /usr/local/go && \
              tar -C /usr/local -xzf /tmp/go.tgz && \
              ln -sf /usr/local/go/bin/go /usr/bin/go

          # (Keep system Go if present for tools, but we rely on downloaded Go above.)
          RUN apk add --no-cache go || (apt-get update && apt-get install -y golang) || true

          WORKDIR /sdk
          COPY . .

          RUN cat <<'EOT' > semantic_test.go
          package main

          import (
            "os"
            "path/filepath"
            "fmt"
            sdk "github.com/atlas-foundry/poml-go-sdk"
          )

          func main() {
            files := []string{}
            filepath.Walk("poml/testdata", func(path string, info os.FileInfo, err error) error {
              if err == nil && !info.IsDir() && filepath.Ext(path) == ".poml" {
                files = append(files, path)
              }
              return nil
            })

            if len(files) == 0 {
              panic("no .poml test files found")
            }

            for _, f := range files {
              data, _ := os.ReadFile(f)
              doc, err := sdk.ParseString(string(data))
              if err != nil {
                panic(fmt.Sprintf("parse error in %s: %v", f, err))
              }
              if err := sdk.Validate(doc); err != nil {
                panic(fmt.Sprintf("semantic error in %s: %v", f, err))
              }
              opts := sdk.ConvertOptions{BaseDir: filepath.Dir(f), MaxImageBytes: 10 << 20, MaxMediaBytes: 10 << 20}
              if _, err := sdk.Convert(doc, sdk.FormatMessageDict, opts); err != nil {
                panic(fmt.Sprintf("convert error in %s: %v", f, err))
              }
            }

            println("OK")
          }
          EOT

          ENV PATH=/usr/local/go/bin:$PATH
          RUN go version && go build semantic_test.go
          EOF

          IMAGE="ghcr.io/${{ github.repository_owner }}/poml-e2e:${TAG}-${ARCH}"
          docker buildx build --platform=linux/${ARCH} --build-arg BASE_IMAGE="${BASE}" --build-arg GO_VERSION="1.25.5" -t "${IMAGE}" --push .

      - name: Run E2E Test
        run: |
          TAG=$(echo "${{ matrix.base }}" | tr ':' '-')
          IMAGE="ghcr.io/${{ github.repository_owner }}/poml-e2e:${TAG}-${{ matrix.arch }}"
          docker run --rm --platform=linux/${{ matrix.arch }} "${IMAGE}"

  # =====================================================
  # 2b. MACOS TESTS (Apple Silicon)
  # =====================================================
  macos:
    name: macOS Build & Test (Apple Silicon)
    runs-on: macos-15
    env:
      GOTOOLCHAIN: go1.25.5

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.5"
          cache: true

      - name: go mod tidy consistency
        run: |
          go mod tidy
          git diff --exit-code || (echo "❌ go mod tidy produced changes"; exit 1)

      - name: Verify formatting
        run: |
          if [ -n "$(gofmt -l .)" ]; then
            echo "❌ gofmt check failed:"
            gofmt -l .
            exit 1
          fi

      - name: Run tests
        run: go test -cover ./...

  # =====================================================
  # 2c. WINDOWS TESTS
  # =====================================================
  windows:
    name: Windows Build & Test
    runs-on: windows-2022
    env:
      GOTOOLCHAIN: go1.25.5

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.5"
          cache: true

      - name: go mod tidy consistency
        shell: bash
        run: |
          go mod tidy
          git diff --exit-code || (echo "❌ go mod tidy produced changes"; exit 1)

      - name: Verify formatting
        shell: bash
        run: |
          if [ -n "$(gofmt -l .)" ]; then
            echo "❌ gofmt check failed:"
            gofmt -l .
            exit 1
          fi

      - name: Run tests
        run: go test -cover ./...

  # =====================================================
  # 2e. LINUX ARM64 SANITY
  # =====================================================
  linux-arm64:
    name: Linux Build & Test (arm64)
    runs-on: ubuntu-24.04-arm
    env:
      GOTOOLCHAIN: go1.25.5

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.5"
          cache: true

      - name: go mod tidy consistency
        run: |
          go mod tidy
          git diff --exit-code || (echo "❌ go mod tidy produced changes"; exit 1)

      - name: Verify formatting
        run: |
          if [ -n "$(gofmt -l .)" ]; then
            echo "❌ gofmt check failed:"
            gofmt -l .
            exit 1
          fi

      - name: Run tests
        run: go test -cover ./...

  # =====================================================
  # 2d. GO VET (Static Checks)
  # =====================================================
  vet:
    name: Go Vet
    runs-on: ubuntu-latest
    env:
      GOTOOLCHAIN: go1.25.5

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.5"
          cache: true

      - name: go mod tidy consistency
        run: |
          go mod tidy
          git diff --exit-code || (echo "❌ go mod tidy produced changes"; exit 1)

      - name: Go Vet
        run: go vet ./...

  # =====================================================
  # 3. DOCS BUILD
  # =====================================================
  docs:
    name: Build Docs
    runs-on: ubuntu-latest
    needs: build

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install Docs Tools
        run: npm install

      - name: Generate docs
        run: |
          mkdir -p build/docs
          node tools/render-mdx.mjs README.md build/docs/index.html

      - name: Docs smoke test
        run: |
          test -s build/docs/index.html

      - name: Upload Docs Artifact
        uses: actions/upload-artifact@v4
        with:
          name: poml-docs
          path: build/docs

  # =====================================================
  # 4. NIGHTLY DOCS
  # =====================================================
  nightly_docs:
    name: Nightly Docs Rebuild
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule'

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - run: |
          npm install
          mkdir -p nightly/docs
          node tools/render-mdx.mjs README.md nightly/docs/index.html
      - uses: actions/upload-artifact@v4
        with:
          name: nightly-docs
          path: nightly/docs

  # =====================================================
  # 5. RELEASE BINARIES (triggered by tag push)
  # =====================================================
  release_binaries:
    name: Build Release Binaries
    runs-on: ubuntu-latest
    needs: [build, e2e, docs]
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.25.5"

      - name: Build binaries
        run: |
          mkdir dist
          GOOS=linux   GOARCH=amd64 go build -o dist/poml-go-sdk-linux-amd64   .
          GOOS=darwin  GOARCH=arm64 go build -o dist/poml-go-sdk-macos-arm64   .
          GOOS=windows GOARCH=amd64 go build -o dist/poml-go-sdk-windows-amd64.exe .

      - name: Upload release assets
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
