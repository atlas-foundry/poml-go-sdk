<poml>
  <meta>
    <id>poml-horse.readme</id>
    <version>0.1.0</version>
    <owner>poml-go-sdk</owner>
  </meta>
  <role>Reference notes for the private poml-horse seed (renderer-agnostic isometric diagram pipeline).</role>
  <task>Capture goals, contents, and caveats for the poml-horse seed artifacts.</task>

  <hint caption="Goal">
    <list>
      <item>POML-first diagram pipeline to emit renderer-agnostic scene JSON and render high-quality isometric (2.5D) graphs.</item>
      <item>MVP: render an isometric graph from POML via deck.gl/luma.gl with solid aesthetics and export SVG/PNG.</item>
    </list>
  </hint>

  <hint caption="Contents">
    <list>
      <item><object data="seed.poml" syntax="text">schema, architecture, work plan, MVP, and a minimal sample &lt;diagram&gt;.</object></item>
      <item><object data="seed.prompt.poml" syntax="text">automation brief for OpenAI/Codex to drive toward ~50% MVP.</object></item>
      <item><object data="scene_exporter_stub.go" syntax="text">tiny proof to parse the seed and emit a scene JSON skeleton (not part of the SDK build).</object></item>
    </list>
  </hint>

  <hint caption="Notes">
    <list>
      <item>Private research; do not publish. Mirrors the outline kept in the Atlas repo but colocated here for poml-go-sdk work.</item>
      <item>Schema is renderer-agnostic; adapters will target deck.gl/luma.gl first, with optional Graphviz/Mermaid/Three.js/glTF routes.</item>
    </list>
  </hint>

  <output-format>Concise POML reference for internal poml-horse seed files.</output-format>
</poml>
